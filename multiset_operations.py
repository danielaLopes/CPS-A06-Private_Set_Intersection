"""from collections import Counter
counterA = Counter(['a','b','b','c'])
counterA
Counter({'b': 2, 'a': 1, 'c': 1})
lst = [Counter(['a','b']), Counter(['a'])]
"""

class Operation:

    def __init__(self):

    def union(self):

    def intersection(self):

    def element_reduction(self):